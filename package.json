{
  "name": "xotiicduck-music-portable",
  "version": "22.0.0",
  "private": true,
  "description": "XotiicDuck Music player, song projects and private release publishing.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4173 --strictPort",
    "build:ui": "node scripts/build-ui.cjs",
    "test": "node scripts/check-syntax.cjs && node scripts/build-ui.cjs --check && node --test tests/*.test.cjs",
    "test:browser": "playwright test",
    "build:public": "node scripts/build-public.cjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^26.1.0",
    "vite": "^8.2.2"
  }
}
