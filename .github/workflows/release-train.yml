name: Completed legacy release train
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  completed:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The Update 13-20 train is complete. Update 22 uses deploy-current.yml for validated deployment."
